{
  "name": "restaurant-randomizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node --experimental-strip-types server/placesProxy.ts",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json && tsc -p tsconfig.sw.json && tsc -p tsconfig.server.json"
  },
  "devDependencies": {
    "@serwist/vite": "^9.5.7",
    "@serwist/window": "^9.5.7",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^25.5.2",
    "serwist": "^9.5.7",
    "svelte": "^5.55.2",
    "svelte-check": "^4.4.6",
    "typescript": "~6.0.2",
    "vite": "^8.0.7"
  }
}
